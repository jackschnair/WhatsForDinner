/*var mysql = require('mysql')
var con = mysql.createConnection({
    host: "127.0.0.1",
    user: "umlwhats_root",
    password: "jdj123",
    database: "umlwhats_wfd"
})

con.connect(function(err){
    if(err) throw err;
    con.query("SELECT * FROM User", function(er, result, fields){
        if(err) throw err;
        console.log(result);
    })
})*/

function sum(a, b) {
    return a + b
}

module.exports = tests




